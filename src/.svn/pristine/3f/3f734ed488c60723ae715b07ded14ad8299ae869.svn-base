import java.util.Arrays;

public class Player extends Character {

    //Le singleton
    private static Player single;

    private String playerName;
	private String[] characterClass = new String[] {"Jedi","Sith","BountyHunters","ImperialAgent", "Mercenary", "Jawa"};


	// Calcul xp gagné
	// (((mobXp*mobLvl)/5)*(((2*mobLvl+10)^2.5)/((MobLvl*lvl+10)^2.5))+1)
	// See also:
	// http://bulbapedia.bulbagarden.net/wiki/Experience
	// http://bulbapedia.bulbagarden.net/wiki/List_of_Pok%C3%A9mon_by_effort_value_yield

    // Constructeur en private pour faire le singleton
	private Player(String playerClass, String playerName) {

		lvl = 1;
		xp = 0;

		if (Arrays.asList(characterClass).contains(playerClass)) {
			//TODO
            //Method method = WhateverYourClassIs.class.getDeclaredMethod(playerCLass);
			//method.invoke();
		}
		else {
			System.err.println("Cette classe n'existe pas !\n");
		}

	}

    //Méthode d'accès au singleton
    public static Player getInstance(String playerClass, String playerName){
        if(single == null)
            single = new Player(playerClass, playerName);

        return single;
    }
	
	// SETTERS
	public void setHp(int hp) {
		this.hp = hp;
	}
	
	// GETTERS
	public int getHp() {
		return this.hp;
	}
	public int getLvl() {
		return this.lvl;
	}

    // Méthodes héritées de Character

}
